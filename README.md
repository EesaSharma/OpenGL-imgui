A simple Dear imgui and OpenGL project that lets you change a color and size of a OpenGL rendered triangle using dear imgui.
To run, open OpenGL.sln and build. Or compile using your favorite compiler.
